import React from 'react';
import {Container} from "react-bootstrap";

const Profile = () => {
    // По API получаем пользователя и рендерим его данные
    return (
        <Container className="mt-3">
            Profile
        </Container>
    );
};

export default Profile;